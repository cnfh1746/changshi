# Codex 全局工作约定

## 语言规范
- **始终用中文**
- **始终使用UTF-8 编码**

## 代码规范
- 禁止全局安装依赖，使用虚拟环境
- 测试脚本放 `tests` 文件夹

## Git 安全
- 删除命令先确认
- 禁止 `git clean -fd`

## 诚实原则
- 看不到的内容直接说明，禁止编造

## 执行原则
- 已授予完全权限时，默认直接执行，不重复征求确认
- 仅在以下情况询问：范围不明确/会改大量文件/涉及删除或潜在破坏
